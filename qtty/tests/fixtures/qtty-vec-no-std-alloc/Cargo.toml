[package]
name = "qtty-vec-no-std-alloc-fixture"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
qtty = { path = "../../..", default-features = false, features = ["alloc"] }

[workspace]
